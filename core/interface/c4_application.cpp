#include "../c4_interface.hpp"
void C4Application::gameModule()
{
    int line = -1;
    do
    {
        // line = this->_interface->askLine();
        // this->_game->currentPlayer()->dropPiece(line);
        // if (this->_game->getStatus() == WON)
        // {
        //     break;
        // }
        // this->_game->nextTurn();
    } while (1);
}